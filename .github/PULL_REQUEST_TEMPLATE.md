## Summary

One paragraph describing what this change does.

## Why

What user problem this solves, or what bug this fixes. Link the issue if
one exists (`Fixes #123`).

## Test plan

- [ ] `npx tsc --noEmit` clean
- [ ] `npm run build` clean
- [ ] `cargo check` + `cargo test --lib --no-default-features` clean
- [ ] Manual: …

## Plan doc

Touches `plan/`? Updated: Yes / No / N/A. If yes, link the doc:
